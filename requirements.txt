# Chess Vision Board State - Python Dependencies

# Deep Learning & Computer Vision
torch>=2.0.0  # PyTorch for YOLO models
ultralytics>=8.0.0  # YOLOv8 implementation
opencv-python>=4.8.0  # OpenCV for image processing
pillow>=10.0.0  # Image loading and manipulation
lerobot[pi]
# PI05 requires patched transformers from lerobot branch (NOT standard PyPI version)
# If transformers check fails, manually install:
#   pip install "git+https://github.com/huggingface/transformers.git@fix/lerobot_openpi"
transformers @ git+https://github.com/huggingface/transformers.git@fix/lerobot_openpi
pyserial
pyzmq>=25.0.0  # ZMQ for low-latency IPC between tele_op and collect_vla_episodes

# Computational Geometry
shapely>=2.0.0  # For IoU calculations and polygon operations

# Chess Engine Integration
python-chess>=1.999  # Chess library for move generation and board representation

# Scientific Computing
numpy>=1.24.0  # Numerical operations

# Kinematics for SO-101 (placo-based FK/IK via lerobot.model.RobotKinematics)
placo  # Used by controls/kinematics.py

# Optional: For visualization and debugging
matplotlib>=3.7.0  # For plotting grid and visualization (optional)

# For SmolVLA
num2words

# For memory-efficient training (8-bit Adam optimizer)
bitsandbytes
